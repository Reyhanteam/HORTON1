@extends('layouts.portal',['title'=>'پیام‌ها','heading'=>'پیام‌ها'])
@section('content')<div class="mx-auto max-w-5xl space-y-6"><h2 class="text-2xl font-bold text-ink">پیام‌ها</h2><section class="glass-card rounded-3xl divide-y divide-line overflow-hidden">@forelse($notifications as $notification)<article class="p-5"><h3 class="font-bold text-ink">{{ $notification->title }}</h3><p class="mt-2 text-sm leading-7 text-ink-soft">{{ $notification->body }}</p><time class="mt-2 block text-xs text-ink-soft">{{ $notification->created_at?->format('Y/m/d H:i') }}</time></article>@empty<div class="p-12 text-center text-sm text-ink-soft">پیامی ندارید.</div>@endforelse</section>{{ $notifications->links() }}</div>@endsection