@extends('layouts.portal',['title'=>$product->name,'heading'=>$product->name])
@section('content')<div class="mx-auto max-w-6xl space-y-6"><a href="{{ route('portal.shop') }}" class="text-sm text-brand">← فروشگاه</a><section class="glass-card rounded-3xl p-7"><span class="rounded-full bg-blue-50 px-3 py-1 text-xs text-brand">{{ $product->category?->name }}</span><h2 class="mt-4 text-3xl font-bold text-ink">{{ $product->name }}</h2><p class="mt-3 text-sm leading-7 text-ink-soft">{{ $product->description }}</p></section><div class="grid gap-5 md:grid-cols-2">@foreach($product->plans as $plan)<article class="glass-card rounded-3xl p-6"><div class="flex items-center justify-between"><h3 class="text-xl font-bold text-ink">{{ $plan->name }}</h3>@if($plan->is_trial)<span class="rounded-full bg-emerald-50 px-3 py-1 text-xs text-emerald-700">آزمایشی</span>@endif</div><p class="mt-3 text-sm text-ink-soft">{{ $plan->description }}</p><div class="mt-5 grid grid-cols-2 gap-3 text-xs"><div class="rounded-xl bg-slate-50 p-3">مدت<strong class="mt-1 block">{{ $plan->duration_value }} {{ $plan->duration_unit }}</strong></div><div class="rounded-xl bg-slate-50 p-3">ظرفیت<strong class="mt-1 block">{{ $plan->capacity_value ? number_format($plan->capacity_value) : 'استاندارد' }}</strong></div></div><div class="mt-6 flex items-center justify-between"><strong class="text-xl text-ink">{{ number_format($plan->price) }} {{ $plan->currency }}</strong><button class="rounded-xl bg-brand px-5 py-3 text-sm font-semibold text-white">انتخاب</button></div></article>@endforeach</div></div>@endsection