@extends('layouts.portal',['title'=>'جزئیات سرویس','heading'=>'جزئیات سرویس'])
@section('content')<div class="mx-auto max-w-5xl space-y-6"><a href="{{ route('portal.services') }}" class="text-sm text-brand">← سرویس‌های من</a><section class="rounded-[2rem] bg-ink p-7 text-white"><p class="text-xs text-blue-200">{{ $service->uuid }}</p><h2 class="mt-2 text-2xl font-bold">{{ $service->plan?->product?->name }}</h2><p class="mt-1 text-sm text-slate-300">{{ $service->plan?->name }}</p><div class="mt-7 grid gap-3 sm:grid-cols-3"><div class="rounded-2xl bg-white/5 p-4">وضعیت<strong class="mt-2 block">{{ $service->status->value ?? $service->status }}</strong></div><div class="rounded-2xl bg-white/5 p-4">انقضا<strong class="mt-2 block">{{ $service->expires_at?->format('Y/m/d') ?? '—' }}</strong></div><div class="rounded-2xl bg-white/5 p-4">ظرفیت<strong class="mt-2 block">{{ $service->capacity ? number_format($service->capacity) : '—' }}</strong></div></div></section><section class="glass-card rounded-3xl p-6"><h3 class="font-bold text-ink">عملیات اخیر</h3><div class="mt-4 divide-y divide-line">@forelse($service->operations as $op)<div class="flex justify-between py-3 text-sm"><span>{{ $op->operation }}</span><span>{{ $op->status }}</span></div>@empty<p class="py-8 text-center text-sm text-ink-soft">رکوردی وجود ندارد.</p>@endforelse</div></section></div>@endsection