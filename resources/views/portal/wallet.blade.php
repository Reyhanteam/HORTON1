@extends('layouts.portal',['title'=>'حساب مالی','heading'=>'حساب مالی'])
@section('content')<div class="mx-auto max-w-6xl space-y-6"><section class="rounded-[2rem] bg-ink p-7 text-white"><p class="text-sm text-blue-200">موجودی حساب</p><p class="mt-3 text-4xl font-bold">{{ number_format($wallet->balance) }} <small class="text-sm">{{ $wallet->currency }}</small></p></section><section class="glass-card rounded-3xl overflow-hidden"><div class="p-6"><h3 class="font-bold text-ink">تاریخچه</h3></div><div class="divide-y divide-line">@forelse($transactions as $tx)<div class="flex justify-between px-6 py-4 text-sm"><div><p class="font-medium">{{ $tx->description ?: $tx->type }}</p><p class="mt-1 text-xs text-ink-soft">{{ $tx->created_at?->format('Y/m/d H:i') }}</p></div><strong>{{ number_format($tx->amount) }}</strong></div>@empty<div class="p-12 text-center text-sm text-ink-soft">رکوردی وجود ندارد.</div>@endforelse</div><div class="p-4">{{ $transactions->links() }}</div></section></div>@endsection