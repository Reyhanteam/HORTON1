@extends('layouts.portal',['title'=>'جزئیات سفارش','heading'=>'جزئیات سفارش'])
@section('content')<div class="mx-auto max-w-5xl space-y-6"><a href="{{ route('portal.orders') }}" class="text-sm text-brand">← سفارش‌ها</a><section class="glass-card rounded-3xl p-6"><div class="flex items-center justify-between"><div><p class="text-xs text-ink-soft">سفارش</p><h2 class="mt-1 text-2xl font-bold text-ink">#{{ $order->id }}</h2></div><span class="rounded-full bg-blue-50 px-3 py-1 text-xs text-brand">{{ $order->status->value ?? $order->status }}</span></div></section><section class="glass-card rounded-3xl p-6"><h3 class="font-bold text-ink">اقلام</h3><div class="mt-5 divide-y divide-line">@foreach($order->items as $item)<div class="flex justify-between py-4 text-sm"><span>{{ $item->name }} × {{ $item->quantity }}</span><strong>{{ number_format($item->total_amount) }} {{ $order->currency }}</strong></div>@endforeach</div><div class="mt-5 border-t border-line pt-5 flex justify-between text-base"><strong>مبلغ نهایی</strong><strong>{{ number_format($order->total_amount) }} {{ $order->currency }}</strong></div></section><section class="glass-card rounded-3xl p-6"><h3 class="font-bold text-ink">فاکتور و پرداخت</h3><p class="mt-3 text-sm text-ink-soft">فاکتور: {{ $order->invoice?->invoice_number ?? '—' }}</p>@foreach($order->payments as $payment)<div class="mt-3 flex justify-between text-sm"><span>{{ $payment->method }}</span><span>{{ $payment->status }}</span></div>@endforeach</section></div>@endsection