@extends('layouts.portal',['title'=>'سفارش‌های من','heading'=>'سفارش‌های من'])
@section('content')<div class="mx-auto max-w-6xl space-y-6"><div><p class="text-sm text-ink-soft">تاریخچه خرید و فاکتورها</p><h2 class="mt-2 text-2xl font-bold text-ink">سفارش‌ها</h2></div><section class="glass-card overflow-hidden rounded-3xl divide-y divide-line">@forelse($orders as $order)<a href="{{ route('portal.orders.show',$order) }}" class="block p-5 hover:bg-slate-50"><div class="flex items-center justify-between gap-4"><div><p class="font-semibold text-ink">سفارش #{{ $order->id }}</p><p class="mt-1 text-xs text-ink-soft">{{ $order->created_at?->format('Y/m/d H:i') }}</p></div><div class="text-left"><span class="rounded-full bg-blue-50 px-3 py-1 text-xs text-brand">{{ $order->status->value ?? $order->status }}</span><strong class="mt-2 block text-sm text-ink">{{ number_format($order->total_amount) }} {{ $order->currency }}</strong></div></div></a>@empty<div class="p-14 text-center text-sm text-ink-soft">هنوز سفارشی ثبت نشده است.</div>@endforelse</section><div>{{ $orders->links() }}</div></div>@endsection